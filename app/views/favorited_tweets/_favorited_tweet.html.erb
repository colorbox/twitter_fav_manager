<%= render favorited_tweet.tweet %>
<%= favorited_tweet.tags.map{|t|t.title} %>
<%= form_for :tag, url: favorite_tag_path(favorite_id: favorited_tweet.id) do |f| %>
  <%= f.text_field :title %>
  <%= f.submit '+' %>
<% end %>
