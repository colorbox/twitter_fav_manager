<% if session[:token] %>
  logout
<% else %>
  <%= link_to 'twitter login', "/auth/twitter?callback_url=#{new_session_url}" %>
<% end %>

<% if @favorited_tweets %>
  <%= render @favorited_tweets %>
  <%= paginate @favorited_tweets %>
<% end %>
